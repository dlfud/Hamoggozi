<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hamoggozi.hamoggozi.dao.GeneralDao">

    <insert id="insertUserBean" parameterType="com.hamoggozi.hamoggozi.dto.UserBean">
        INSERT INTO USER (ID, PW, NAME, PHONE, AUTH, INSERTBY, INSERTDATE, UPDATEBY, UPDATEDATE)
        VALUES (#{id}, #{pw}, #{name}, #{phone}, #{auth}, #{crudUid}, SYSDATE(), #{crudUid}, SYSDATE())
    </insert>

    <select id="getUser" parameterType="String" resultType="com.hamoggozi.hamoggozi.dto.UserBean">
        SELECT U.UID, U.ID, U.PW, U.NAME, U.PHONE, U.AUTH, U.INSERTDATE, U.UPDATEDATE
        FROM USER U
        WHERE U.ID = #{id}
    </select>

</mapper>

